node_modules/
dist/
*.log
.env
.env.*
!.env.example

# Personal Claude Code settings (local MCP approvals) — not committed
.claude/settings.local.json

# Internal instruction / scenario docs — kept local, not committed
docs/

# Large generated media — keep the folders, ignore their contents
keyframes/*
outputs/*
!keyframes/.gitkeep
!outputs/.gitkeep

.DS_Store
