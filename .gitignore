node_modules/
dist/
*.log
.env
.env.*
!.env.example

# Personal Claude Code settings (local MCP approvals) — not committed
.claude/settings.local.json

# Per-project instruction / scenario docs & spend log — kept local, not committed
projects/*/docs/
projects/*/credit-log.md

# Per-project large generated media — ignore contents; scenes.ts + prompts/ stay tracked
projects/*/keyframes/
projects/*/outputs/
projects/*/references/

.DS_Store
